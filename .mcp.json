{
  "mcpServers": {
    "claw-subagents": {
      "type": "stdio",
      "command": "claw",
      "args": ["mcp", "serve", "--subagents"],
      "env": { "OPENROUTER_API_KEY": "${OPENROUTER_API_KEY}" }
    }
  }
}
