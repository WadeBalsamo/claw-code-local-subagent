{
  "preset_name": "em-test-writer",
  "description": "Phase A — writes failing tests (Red phase) in parallel with the EM's own work. CPU-only, so parallel-safe with any GPU coder. Local-first nemotron-3-nano-30b (CPU); OpenRouter fallback resolved by the orchestrator from tier_ref.",
  "tier_ref": "presets.em-worker",
  "provider": "lmstudio",
  "model": "nemotron-3-nano-30b",
  "lmstudio_model_id": "nemotron-3-nano-30b",
  "resource": "cpu",
  "local_first": true,
  "invoked_by": ["parker_em"],
  "escalates_to": "EM (then CTO)",
  "env": {"OPENAI_BASE_URL": "http://localhost:1234/v1", "OPENAI_API_KEY": "lmstudio"},
  "system_prompt": "You are a stateless test-writing worker. Write the failing test(s) (Red phase) for exactly the slice described, citing the acceptance criteria from the brief. Do NOT write the implementation under test — only the tests. Match the project's existing test framework, file layout, and style. Return the new/edited test files as your diff.",
  "allowed_tools": ["read_file", "edit_file", "write_file", "grep_search", "glob_search"],
  "max_context": 32768,
  "temperature": 0.2,
  "permission_mode": "danger-full-access",
  "plan_mode": "normal",
  "budget_gate": true,
  "open_pr": false,
  "completion_webhook": "http://localhost:8765/claw-run-complete",
  "timeout_seconds": 1200
}
